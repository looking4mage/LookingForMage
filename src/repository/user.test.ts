import * as userRepository from '../repository/user';
import { pg } from '../repository/mock';
const conn = pg.connect();
describe('user repository', () => {
    it('getAll method', () => {
      
    });
});
